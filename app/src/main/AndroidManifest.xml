<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.fabiocarvalho.scruiz" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:appComponentFactory"
        android:appComponentFactory="whateverString">

        <!-- PRINCIPAL -->
        <activity
            android:name=".ScruizActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- CHOOSER - ROTEADOR PAR LOGIN Google / E-mail / Anonimo -->
        <activity android:name=".authenticator.ChooserActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ScruizActivity" />
        </activity>
        <!-- GOOGLE -->
        <activity android:name=".authenticator.GoogleActivity"
            android:parentActivityName=".ScruizActivity">
        </activity>

    </application>

</manifest>